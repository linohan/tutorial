#include "test.h"
// g++ -g -Wall -std=c++11 test.cpp -o test

int main(int argc, char const* argv[]) {
  // vector<tuple<double, double, double>> v_pa;
  // auto pa = make_tuple(0.0, 0.0, 0.0);
  // for (size_t i = 0; i < 5; i++) {
  //   v_pa.push_back(pa);
  // }
  // for (vector::iterator it : v_pa) {
  //   get<0>(it) = 1.0;
  // }

  // for (auto it : v_pa) {
  //   cout << get<0>(it) << endl;
  // }
  TestVector();
}